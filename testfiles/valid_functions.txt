interface Function<x|y> {
  call maps (x) to (y)
}

interface Read<|x> {
  x allows missing

  get maps () to (x)
}

interface Updater {
  update<x> maps (x) to (x)
    x allows missing
    x requires Read<x>

  nothing maps () to ()
}
