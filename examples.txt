// A contrived Iterator.
interface Iterator<|x> {
  inherits Reader<x>
  inherits Writer<x>
  x requires Iterator<x>
}

interface Writer<x|> {
}

interface Reader<|x> {
}

concrete Queue<x> {
  /*
   * Testing block comments.
   */
  inherits Writer<x>
  /*
   * Testing block comments.
   */
  inherits Reader<x>
  x requires Writer<Writer<x>>
}

interface Function<x|y> {
  inherits Iterator<Iterator<y>>
  inherits Writer<x>
}
